[build]
  builder = "DOCKERFILE"
  dockerfilePath = "Dockerfile"

[deploy]
  startCommand = "npx prisma db push --skip-generate && node src/start.js"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3

[[services]]
  name = "piratesocial-hub"
  protocol = "TCP"
  internalPort = 3000

[[services.healthchecks]]
  path = "/"
  protocol = "HTTP"
  interval = 30
  timeout = 5
